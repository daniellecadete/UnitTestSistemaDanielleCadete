node {
    stage('Build') {
      node() {
        echo 'Master'
        sh 'hostname'
        deleteDir()
        checkout scm
        sh 'echo "res" > result'
        stash includes: '**/result', name: 'app'
        
      }
     }  
      
    stage("Test") {
      node('slave') {
        echo 'Slave'
	unstash 'app'
        sh 'hostname'
        sh "mvn clean install"
	//unstash 'app'
	//echo 'Testes realizados!'
      }
    }     
}
